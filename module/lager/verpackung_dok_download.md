# lager/verpackung_dok_download.php – Verpackungs-Dokument ausliefern

Route: `?p=verpackung_dok&id=<id>` (interne Rollen)

Liefert eine an einer Verpackung hinterlegte Datei (`verpackung_dokument`) aus `data/uploads` (außerhalb `public`). 404, wenn Eintrag oder Datei fehlen.
